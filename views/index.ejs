<!DOCTYPE html>
<html>
<head>
  <title>Google Meet</title>
  <!-- <style>
    /* CSS styles */
    body {
      margin: 0;
      padding: 0;
    }

    #header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 10px;
    }

    #logo {
      width: 40px;
      height: 40px;
    }

    #login-button {
      padding: 8px 16px;
      background-color: #4285f4;
      color: #fff;
      text-decoration: none;
      border-radius: 4px;
    }

    #profile-pic {
      width: 40px;
      height: 40px;
      border-radius: 50%;
    }

    #main-content {
      display: flex;
      justify-content: center;
      align-items: center;
      height: 80vh;
    }

    #meet-buttons {
      display: flex;
      flex-direction: column;
      align-items: flex-start;
      margin-right: 50px;
    }

    .button {
      margin-bottom: 20px;
      padding: 10px 20px;
      background-color: #4285f4;
      color: #fff;
      text-decoration: none;
      border-radius: 4px;
    }

    #meet-link-input {
      width: 300px;
      padding: 8px;
      border: 1px solid #ccc;
      border-radius: 4px;
    }

    #image {
      max-width: 400px;
      max-height: 400px;
    }
  </style> -->
  <link rel="stylesheet" href="index.css">
</head>
<body>
  <header id="header">
    <% if (user) { %>
        <img id="profile-pic" src="<%= user.picture %>" alt="Profile Picture">
    <% } else { %>
        <a id="login-button" href="/auth/google">Login</a>
    <% } %>
    <img id="logo" src="./video-chat.png" alt="Google Meet Logo">
    <!-- <a id="login-button" href="#">Login</a> -->
  </header>

  <div id="main-content">
    <div id="meet-buttons">
      <a class="button" href="/room">Start a Meet</a>
      <a class="button" href="#">Schedule a Meet</a>
    </div>

    <input id="meet-link-input" type="text" placeholder="Enter meet link">

    <img id="image" src="path_to_image.png" alt="Image">
  </div>
</body>
</html>
